<local:DetailPageBase
    x:Class="Kliva.Views.ClubDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Kliva.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Kliva.Controls"
    mc:Ignorable="d"
    Loaded="Page_Loaded"
    DataContext="{Binding ClubDetail, Source={StaticResource Locator}}">

    <Grid
        x:Name="PageRoot"
        x:DeferLoadStrategy="Lazy"
        Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <controls:ClubDetailControl />
    </Grid>
</local:DetailPageBase>
